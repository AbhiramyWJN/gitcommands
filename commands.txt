git init => for git initialization
git status => to list untracked files

git add -A => adding all files to staging area
gti commit -m "message" =>
moving staged files to local repository

git config --global user.name "name of user"

gti remote add name path of remote
